apiVersion: v1
kind: Service
metadata:
  name: newpython-frontend
  namespace: storefront-dev012
  labels:
    app.kubernetes.io/name: newpython-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: newpython-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
